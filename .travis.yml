# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages
install:
  ## For installing all CRAN dependencies using package's DESCRIPTION file
  - ./travis-tool.sh install_deps
